import React from "react";

const Home = () => {
  return (
    <div className="container">
      <h4 className="center">Home</h4>
      <p>
        {" "}
        React is a library for building composable user interfaces. It
        encourages the creation of reusable UI components that present data that
        changes over time. React is a powerful JavaScript UI library for
        creating modern applications.
      </p>
    </div>
  );
};

export default Home;
